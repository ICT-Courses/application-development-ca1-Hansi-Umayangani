<Window x:Class="AquaPOS.User_Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AquaPOS"
        mc:Ignorable="d"
        Icon="pack://application:,,,/resources/Images/App_Logo/AquaPOS_Logo_Icon_New.ico"
        Title="User Login" Height="720" Width="1280" ResizeMode="CanResize" WindowStartupLocation="CenterScreen">

    <Window.Background>
        <ImageBrush ImageSource="pack://application:,,,/resources/Images/Background_Images/Main_Background_Image.png"
         Stretch="Fill"/>
    </Window.Background>
    
    <Window.Resources>
        <!-- Rounded TextBox and PasswordBox Style -->
        <Style x:Key="RoundedTextBoxStyle" TargetType="Control">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border CornerRadius="8" BorderBrush="#FF0807A0" BorderThickness="2.5" Background="White">
                            <ScrollViewer Margin="5" x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded Button Style -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border 
                    x:Name="border"
                    Background="{TemplateBinding Background}" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="{TemplateBinding BorderThickness}" 
                    CornerRadius="20"
                    SnapsToDevicePixels="True">
                            <ContentPresenter 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center" 
                        RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Hover: Scale up -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(Button.RenderTransform).(ScaleTransform.ScaleX)"
                                 To="1.05" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(Button.RenderTransform).(ScaleTransform.ScaleY)"
                                 To="1.05" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(Button.RenderTransform).(ScaleTransform.ScaleX)"
                                 To="1" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(Button.RenderTransform).(ScaleTransform.ScaleY)"
                                 To="1" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>

                            <!-- Click: Quick bounce effect -->
                            <EventTrigger RoutedEvent="Button.Click">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(Button.RenderTransform).(ScaleTransform.ScaleX)"
                             To="0.95" Duration="0:0:0.05" AutoReverse="True"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(Button.RenderTransform).(ScaleTransform.ScaleY)"
                             To="0.95" Duration="0:0:0.05" AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Height="720">
        <!-- Background Image -->
        <Image Source="pack://application:,,,/resources/Images/App_Logo/AquaPOS_Logo.png" 
               Height="232" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,40,0,0" Stretch="Fill" Width="398"/>

        <!-- Username Label & UsernameTextBox -->
        <Label Content="Username" FontSize="16" FontWeight="Bold" Foreground="White" 
               HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,299,0,0" Width="86"/>
        <TextBox x:Name="UsernameTextBox" Width="306" Height="36" FontSize="14"
                 Style="{StaticResource RoundedTextBoxStyle}" Margin="0,329,0,0" HorizontalAlignment="Center" VerticalAlignment="Top" 
                 TextChanged="UsernameTextBox_TextChanged"/>

        <!-- Password Label & PasswordBox -->
        <Label Content="Password" FontSize="16" FontWeight="Bold" Foreground="White"
               HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,387,0,0" Width="82"/>
        <PasswordBox x:Name="PasswordBox" Width="306" Height="36" FontSize="14"
                     Style="{StaticResource RoundedTextBoxStyle}"
                     HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,416,0,0"/>
        <TextBox x:Name="PasswordTextBox" Width="306" Height="36" FontSize="14"
                 Style="{StaticResource RoundedTextBoxStyle}"
                 HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,416,0,0" 
                 Visibility="Collapsed"/>

        <!-- Show Password Checkbox -->
        <CheckBox x:Name="ShowPasswordCheckbox" Content="Show Password" Foreground="White" FontSize="12"
                  HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,457,488,0" 
                  Checked="ShowPasswordCheckbox_Checked" Unchecked="ShowPasswordCheckbox_Unchecked" Width="104"/>

        <!-- Login Button -->
        <Button x:Name="LoginButton" Content="LOGIN" Width="86" Height="45" FontSize="14" FontWeight="Bold" 
                Background="Aquamarine" Foreground="#FF020203" BorderThickness="0"
                Style="{StaticResource RoundedButtonStyle}"
                HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,495,0,0" Click="LoginButton_Click"/>

        <!-- Create Account Button -->
        <Button x:Name="CreateAccountButton" Content="Create Account" Width="157" Height="45" FontSize="16" FontWeight="Bold" 
                Background="#FF00CCEE" Foreground="Black" BorderThickness="0"
                Style="{StaticResource RoundedButtonStyle}"
                HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,584,0,0" Click="CreateAccountButton_Click" BorderBrush="#FF0F35C8"/>
    </Grid>
</Window>
