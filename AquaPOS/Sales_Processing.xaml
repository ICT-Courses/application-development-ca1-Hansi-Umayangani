<Window x:Class="AquaPOS.Sales_Processing"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AquaPOS"
        mc:Ignorable="d"
        Icon="pack://application:,,,/resources/Images/App_Logo/AquaPOS_Logo_Icon_New.ico" 
        Title="Sales Processing" Height="720" Width="1280" ResizeMode="CanResize" WindowStartupLocation="CenterScreen">

    <Window.Background>
        <ImageBrush ImageSource="pack://application:,,,/resources/Images/Background_Images/Main_Background_Image.png"
            Stretch="Fill"/>
    </Window.Background>

    <Window.Resources>
        <!-- Rounded Button Style -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="20"
                                SnapsToDevicePixels="True">
                            <ContentPresenter 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Center" 
                                RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded TextBox Style -->
        <Style x:Key="RoundedTextBoxStyle" TargetType="Control">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border CornerRadius="8" BorderBrush="#FF0807A0" BorderThickness="2.5" Background="White">
                            <ScrollViewer Margin="5" x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Grid.Column="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Panel -->
        <Border Background="#FF031C36" Width="260" HorizontalAlignment="Left">
            <StackPanel>

                <!-- Side Bar -->
                <!-- Logo -->
                <Image Source="pack://application:,,,/resources/Images/App_Logo/AquaPOS_Logo.png"
                   Width="244" Height="176"/>

                <!-- Search  Product Details Panel of Side Bar -->
                <Border Background="#FFCAD8E5" BorderBrush="#FF0807A0" BorderThickness="2.5" Width="220" CornerRadius="10" HorizontalAlignment="Left" Margin="20,0,0,0" Height="440">
                    <StackPanel>
                        <WrapPanel Margin="5,5,5,5">

                            <!-- Search Title -->
                            <TextBlock Text="PRODUCT DETAILS" FontSize="16" FontWeight="Bold" Foreground="Black" Margin="20,20,20,20" TextAlignment="Left"/>

                            <StackPanel Orientation="Horizontal" Margin="16,0,0,10">
                                <Button Width="32" Height="32" Margin="2,0,0,0" BorderBrush="#FF0807A0" BorderThickness="2.5" Click="SearchProductDetails_Click">
                                    <Image Source="/Resources/Images/Button_Icons/Search_Icon.png" Stretch="Uniform" Height="20" Width="20" />
                                </Button>
                                <ComboBox x:Name="cmbSearchProductDetails"
                                          Width="146" Height="32"
                                          IsEditable="True"
                                          StaysOpenOnEdit="True"
                                          BorderBrush="#FF0807A0" BorderThickness="2.5"
                                          HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                                          Loaded="CmbSearchProductDetails_Loaded"
                                          IsTextSearchEnabled="False"
                                          PreviewTextInput="CmbSearchProductDetails_PreviewTextInput"
                                          SelectionChanged="CmbSearchProductDetails_SelectionChanged"/>
                            </StackPanel>


                            <!-- Result Fields -->
                            <StackPanel Width="200" Height="300" Margin="5,15,5,5">

                                <!-- Product Name -->
                                <StackPanel Margin="0,0,0,20">
                                    <TextBlock Text="Product Name" FontSize="14" Foreground="Black" Height="24" Width="180"/>
                                    <TextBox x:Name="txtProductNameResult" Width="180" Height="32" BorderBrush="#FF0807A0" BorderThickness="2.5"
                                        Style="{StaticResource RoundedTextBoxStyle}" TextChanged="TxtProductNameResult_TextChanged"/>
                                </StackPanel>

                                <!-- Category -->
                                <StackPanel Margin="0,0,0,20">
                                    <TextBlock Text="Category" FontSize="14" Foreground="Black" Height="24" Width="180"/>
                                    <TextBox x:Name="txtCategoryResult" Width="180" Height="32" BorderBrush="#FF0807A0" BorderThickness="2.5"
                                         Style="{StaticResource RoundedTextBoxStyle}" TextChanged="TxtCategoryResult_TextChanged"/>
                                </StackPanel>

                                <!-- Unit Price -->
                                <StackPanel Margin="0,0,0,20">
                                    <TextBlock Text="Unit Price" FontSize="14" Foreground="Black" Height="22" Width="180"/>
                                    <TextBox x:Name="txtUnitPriceResult" Width="180" Height="32" BorderBrush="#FF0807A0" BorderThickness="2.5"
                                         Style="{StaticResource RoundedTextBoxStyle}" TextChanged="TxtUnitPriceResult_TextChanged"/>
                                </StackPanel>

                                <!-- Available Quantity -->
                                <StackPanel Margin="0,0,0,20">
                                    <TextBlock Text="Available Quantity" FontSize="14" Foreground="Black" Height="22" Width="180"/>
                                    <TextBox x:Name="txtAvailableQtyResult" Width="180" Height="32" BorderBrush="#FF0807A0" BorderThickness="2.5"
                                         Style="{StaticResource RoundedTextBoxStyle}" TextChanged="TxtAvailableQtyResult_TextChanged"/>
                                </StackPanel>
                            </StackPanel>
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <!-- Exit Button with Underlined Text -->
                <Button 
                    Width="100"
                    Height="40"
                    FontSize="16"
                    Background="Transparent"
                    Foreground="White"
                    BorderBrush="Transparent"
                    BorderThickness="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="0,0,0,0" Click="ExitButton_Click">
                    <TextBlock Text="EXIT" 
                       Foreground="White"
                       TextDecorations="Underline"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
                </Button>

            </StackPanel>
        </Border>

        <Border Grid.Column="2" 
            Background="#FFCBD6E2" 
            BorderBrush="#FF0807A0" 
            BorderThickness="2.5" 
            CornerRadius="10" 
            Padding="20" 
            Margin="20" 
            HorizontalAlignment="Left" Width="220">


            <StackPanel Margin="0,3,0,0">

                <TextBlock Text="BILLING" FontSize="16" FontWeight="Bold" Foreground="Black" Margin="2,10,0,0" TextAlignment="Left"/>

                <!-- Bill Date Label & DatePicker -->
                <Label Content="Bill Date" Foreground="Black" FontSize="16" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,30,0,0"/>
                <DatePicker x:Name="BillDatePicker" Width="170" Height="32" FontSize="14" 
                            BorderBrush="#FF0807A0" HorizontalAlignment="Center"/>

                <!-- Product Name Label & TextBox -->
                <!-- Replace Product Name Label & TextBox with Search ComboBox -->
                <Label Content="Product Name" Foreground="Black" FontSize="16" FontWeight="Bold"
                        HorizontalAlignment="Center" Margin="0,20,0,0"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <ComboBox x:Name="cmbSearchProduct"
                              Width="138" Height="32"
                              IsEditable="True"
                              StaysOpenOnEdit="True"
                              BorderBrush="#FF0807A0" BorderThickness="2.5"
                              HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                              Loaded="CmbSearchProduct_Loaded"
                              IsTextSearchEnabled="False"
                              PreviewTextInput="CmbSearchProduct_PreviewTextInput"
                              SelectionChanged="CmbSearchProductDetails_SelectionChanged"/>
                    <Button Width="32" Height="32" Margin="2,0,0,0" BorderBrush="#FF0807A0" BorderThickness="2.5" Click="SearchProduct_Click">
                        <Image Source="/Resources/Images/Button_Icons/Search_Icon.png" Stretch="Uniform" Height="20" Width="20" />
                    </Button>
                </StackPanel>


                <!-- Quantity Label & TextBox -->
                <Label Content="Quantity" Foreground="Black" FontSize="16" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,0"/>
                <TextBox x:Name="QuantityTextBox" Width="170" Height="33" FontSize="14"
                         Style="{StaticResource RoundedTextBoxStyle}"
                         HorizontalAlignment="Center" BorderBrush="#FF0807A0"/>

                <!-- Add to Bill Button -->
                <Button x:Name="AddToBillButton" Content="Add to Bill" Width="130" Height="34" FontSize="14" FontWeight="Bold" 
                        Foreground="Black" Background="#FF8BF1ED" BorderBrush="#FF3F43CE" BorderThickness="1"
                        Style="{StaticResource RoundedButtonStyle}"
                        HorizontalAlignment="Center" Margin="0,30,0,0" Click="AddToBillButton_Click"/>

                <!-- Total Amount Display -->
                <StackPanel Margin="0,60,0,0" HorizontalAlignment="Center">
                    <Label Content="Total Amount (Rs.) :" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="TotalAmountTextBlock" Text="0.00" FontSize="16" FontWeight="Bold" 
               Foreground="#FF0807A0" HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Print Bill Button -->
                <Button x:Name="PrintBillButton" Content="Print Bill" Width="130" Height="34" FontSize="14" FontWeight="Bold" 
                        Foreground="Black" Background="#FF9DFF59" BorderBrush="#FF15A90E" BorderThickness="1"
                        Style="{StaticResource RoundedButtonStyle}"
                        HorizontalAlignment="Center" Margin="0,20,0,0" Click="PrintBillButton_Click"/>
            </StackPanel>
        </Border>

        <!-- Billing Table -->
        <Grid Grid.Column="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="866"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Border 
                BorderBrush="#FF0807A0" 
                BorderThickness="2.5" 
                CornerRadius="10" 
                Padding="2" 
                Background="White" 
                Margin="260,20,20,20" Grid.ColumnSpan="2">
                <DataGrid x:Name="BillDataGrid"
                  AutoGenerateColumns="False"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  RowHeight="24"
                  Background="White"
                  AlternatingRowBackground="#FFEFEFEF"
                  FontSize="12"
                  FontFamily="Segoe UI"
                  SelectionChanged="StockDataGrid_SelectionChanged_1"
                  BorderBrush="#FFD7E3FA"
                  BorderThickness="1"
                  HorizontalGridLinesBrush="Transparent"
                  VerticalGridLinesBrush="Transparent" Margin="5,5,5,5"
                  Grid.Row="2" Grid.Column="0"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  ItemsSource="{Binding Stocks}"
                  VerticalScrollBarVisibility="Visible">

                    <DataGrid.Resources>
                        <Style TargetType="{x:Type DataGridCell}">
                            <Setter Property="BorderBrush" Value="#FFD3D3D3"/>
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="Padding" Value="5,0,5,0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>

                        <Style TargetType="{x:Type DataGridColumnHeader}">
                            <Setter Property="Background" Value="#FF093564"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Height" Value="30"/>
                            <Setter Property="BorderBrush" Value="#FFD3D3D3"/>
                            <Setter Property="BorderThickness" Value="0,0,2,1"/>
                        </Style>

                        <Style x:Key="CenterTextStyle" TargetType="{x:Type TextBlock}">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.Resources>

                    <!-- ✅ 2. Columns -->
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Product ID" Binding="{Binding ProductID}" Width="100"
                        ElementStyle="{StaticResource CenterTextStyle}"/>
                        <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="160"
                        ElementStyle="{StaticResource CenterTextStyle}"/>
                        <DataGridTextColumn Header="Unit Price (Rs.)" Binding="{Binding UnitPrice}" Width="120"
                        ElementStyle="{StaticResource CenterTextStyle}"/>
                        <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="100"
                        ElementStyle="{StaticResource CenterTextStyle}"/>
                        <DataGridTextColumn Header="Total Price (Rs.)" Binding="{Binding TotalPrice}" Width="120"
                        ElementStyle="{StaticResource CenterTextStyle}"/>

                        <DataGridTemplateColumn Header="Actions" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <!-- Delete Button -->
                                        <Button Name="DeleteButton" 
                                                Click="DeleteButton_Click"
                                                CommandParameter="{Binding}"
                                                ToolTip="Delete"
                                                Background="Transparent"
                                                BorderBrush="Transparent"
                                                Cursor="Hand"
                                                Padding="0,0,10,0">
                                                <Image Source="pack://application:,,,/Resources/Images/Button_Icons/Delete_Button_Icon.png"
                                                       Width="20" Height="20"
                                                       Stretch="Uniform"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>

                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</Window>
